/* main function - first thing that is run for every executable Rust program
 * It returns nothing and has no arguments.
 */
fn main() {
    // println! is a macro not a function. This is indicated by the ! symbol.
    println!("Hello, world!");
}

// Rust is a compiled language: `rustc main.rs` to compile it.